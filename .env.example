# Supabase project settings — copy this file to `.env` and fill in your
# project's values (Supabase dashboard → Project Settings → API).
#
# NEVER commit the real `.env` file or a service_role key. Only the anon
# (public) key belongs in the browser bundle — Row Level Security policies
# in supabase/schema.sql keep every user's data private.

VITE_SUPABASE_URL=https://okzubtqvsmrecltexgya.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_TGl9E-SbQnBaNuIJyQJv8w_9pDB_ijy
